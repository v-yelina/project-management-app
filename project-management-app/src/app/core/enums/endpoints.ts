export enum AuthEndpoint {
  SIGN_IN = '/auth/signin',
  SIGN_UP = '/auth/signup',
}

export enum UsersEndpoint {
  USERS = '/users',
}

export enum BoardsEndpoint {
  BOARDS = '/boards',
  BOARDS_SET = '/boardsSet',
}
